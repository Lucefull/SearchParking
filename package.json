{
  "name": "searchparking",
  "version": "1.0.0",
  "description": "",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "json-server --watch server.json --host localhost --port 8081 --no-cors"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "jquery": "^3.7.0",
    "json-server": "^0.17.3"
  }
}